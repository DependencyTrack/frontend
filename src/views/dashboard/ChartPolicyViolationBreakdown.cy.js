import ChartPolicyViolationBreakdown from '@/views/dashboard/ChartPolicyViolationBreakdown.vue';

describe('ChartPolicyViolationBreakdown', () => {
  it('mounts successfully', () => {
    cy.mount(ChartPolicyViolationBreakdown);
  });
});
